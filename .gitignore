node_modules/
dist/
*.tsbuildinfo
coverage/

.env
.env.*
!.env.example

npm-debug.log*
.DS_Store

# Internal planning documents live in a separate private repository and must
# never be committed here. Ignoring them stops the careless copy before it is
# staged; .github/workflows/verify-publishable.yml catches the rest.
mcp-server-plan.md
EXECUTION.md
PRD.md
TDD.md
COSTS.md
chance-history.md
marketing-*.md

# Lambda build artifact (npm run build:lambda)
lambda-dist/

# Deploy artifact (aws cloudformation package). Generated per environment,
# names an S3 object that only exists in one account, and is rewritten on
# every deploy - committing one would pin a stale bundle into the template.
infra/sam/.packaged-*.yaml
